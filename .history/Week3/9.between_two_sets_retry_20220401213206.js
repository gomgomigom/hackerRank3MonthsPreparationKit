'use strict';

function getTotalX(a, b) {
  let LCM = 1;
  for (value of a){
    value % LCM === 0 ? LCM = value
  }

  const GCD = ;

}

const a = [2, 6];
const b = [24, 36];

console.log(getTotalX(a, b));
