'use strict';

function separateNumbers(s) {
  const arr = s.split('');
  const length = s.split('').length;
  console.log(arr);
  console.log(length);
  // Write your code here
}

const s = '99100';

separateNumbers(s);
