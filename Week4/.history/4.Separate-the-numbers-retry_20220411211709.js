'use strict';

function separateNumbers(s) {
  const arr = s.split('');
  const length = arr.length;
  console.log(arr);
  console.log(length);
  // Write your code here
}

const s = '123';

separateNumbers(s);
